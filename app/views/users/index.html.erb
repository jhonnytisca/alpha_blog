<% content_for :title, "BLoggers" %>
<div class="container mt-5">
  <h3 class="mb-5">Alpha Bloggers</h3>
  <% if @users.empty? %>
    <p class="mt-3 mb-5">No users to show</p>
  <% end %>
  <%= render 'user' %>
</div>
